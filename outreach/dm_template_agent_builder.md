# DM Templates — Agent Builder Outreach

---

## Version 1: Cold DM

Hey — I built a reputation scoring API for AI agent wallets on Base. It scores any wallet 0-100 across five dimensions (payment reliability, economic viability, identity, behavior, and capability) so agents can assess counterparty trust before transacting. Free tier, live right now. You can try it in one line:

```
curl https://djdagentscore.dev/v1/score/basic?wallet=0xYOUR_WALLET
```

Would love to hear if this is useful for what you're building.

---

## Version 2: Warm DM

Hey — been following [PROJECT_NAME], really like what you're doing with [SPECIFIC_THING]. I built something that might be useful for you: a reputation scoring API for AI agent wallets on Base. It gives agents a way to check trust scores before transacting — five scoring dimensions, sybil detection, sub-200ms responses. There's a free tier and a Python SDK (`pip install djd-agent-score`), plus an MCP server on npm.

Would love to chat about how this could plug into your agent workflow. Quick test:

```
curl https://djdagentscore.dev/v1/score/basic?wallet=0xYOUR_WALLET
```

---

## Version 3: Follow-Up (No Response)

Following up — I know DMs are noisy. Quick version: DJD Agent Score is a live API that scores AI agent wallets 0-100 on Base. Free tier, Python SDK on PyPI, MCP server on npm. If your agents are transacting on-chain and need a way to vet counterparties, this is built exactly for that.

Happy to do a quick walkthrough or just answer questions async. No pressure either way.

---

## Notes

- Personalize Version 2 with specifics about their project
- Don't send Version 3 less than 5 days after the initial DM
- If they respond with interest, offer a live API key for the paid tier (full score breakdown, history, batch scoring)
- Keep it short — these are builders, they respect brevity
